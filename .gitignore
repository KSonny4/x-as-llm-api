.env
__pycache__/
target/
node_modules/
*.log
feedback.jsonl

# built binaries
zencli/zencli
zencli/zencli-linux

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
