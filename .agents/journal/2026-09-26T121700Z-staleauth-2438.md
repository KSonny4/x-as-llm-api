# Journal: drop inline registry auth (2026-09-26T1217Z, lane STALEAUTH-REPO)

## Live state
- Lane STALEAUTH-REPO (track 2 of Two-track fix, issue #2438): remove every
  task-level docker `auth{}` + `dr_user`/`dr_pass` vars from Nomad specs.
- Registry auth is client-level on the node since 12:05Z
  (`/opt/nomad/docker-auth.json`, docker plugin auth config); task-level
  auth overrides it, and the rotated password breaks any spec carrying it.

## Shipped (worktree .worktrees/staleauth-2438, branch
fix/drop-inline-registry-auth-2438)
- keeper.nomad.hcl: 2 auth blocks + dr_user/dr_pass vars removed.
- keeper-probe-shell, probe-keyround, probe-keyround-manual,
  zencli-nomad.hcl: 1 auth block each + vars removed; -var=dr_pass header
  examples replaced with client-level-auth note.
- Left alone: keeper-probe.nomad.hcl, keeper-probe-1route.nomad.hcl
  (no docker auth), keeper-alloy.nomad.hcl (basic_auth is Grafana Cloud).
- `grep var.dr_user/var.dr_pass` repo-wide: zero references remain.

## Settled truths (measured, not claimed)
- keeper-staging-slim.nomad.hcl and docs/STAGING-SLIM-PLAN.md exist only
  as untracked files in the dirty main checkout — never committed to
  origin/main, no git history. Untouched; reported as lane deviation.
- No .github/workflows in this repo: no CI to watch.

## Blockers / next
- PR opened unmerged per lane rules; counterpart track owns the live side.
