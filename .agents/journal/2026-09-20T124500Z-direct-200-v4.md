# 2026-09-20T~12:45Z — v4: DIRECT 200 (goal GREEN); v3 + drained-key WITHDRAWN

## The flip
`pi --provider opencode-zen-free --model big-pickle` → `PI-ZEN-DIRECT`,
exit 0 (goal call 4 of 5). The reuse-first direct provider WORKS end to
end. M2 GREEN; blessed path: opencode-zen-free/big-pickle, no keeper hop.

## Withdrawals (auditor was right on both)
1. "Drained Petr bucket": goal call 5 (Petr key direct-HTTPS) → 403
   gate-shape, never 429. Same key as Call 1, different path, different
   verdict → the 429 shape comes from the KEEPER RELAY PATH, not the key.
   Cause of Call 1 re-attributed to relay-path gate.
2. Verdict-v3 "unforgeable from outside the binary": falsified by Call 4
   (pi Bun-stack HTTPS 200). HTTP reuse IS possible. zencli remains a
   valid instrument + fallback client, not a dead end. The undetermined
   remainder is only WHICH request element gates (prime suspect: keeper
   strips CLI identity upstream — urllib UA, Content-Type+bearer only).

## Shape map (same account, same hour — path/fingerprint is the variable)
- CLI binary → 200 · pi-direct (Bun+CLI identity) → 200
- keeper relay (urllib, no identity) → 429 FreeUsageLimit
- bare/SDK/Go replays (Bun/node/curl/uTLS, partial identity) → 403 FreeTierError
- paid models → CreditsError/401 (no billing, unchanged)

## Cap + hygiene
Goal vendor calls FINAL 5/5 (no violation, no further calls). Full-repo +
history secret scan: zero hits (patterns only). Petr-key rotation still
owed (owner). Keeper relay left ungated-unfixed: keeper serving changes
are OUT of this goal's scope; seed reorder stays as deployed hygiene.
