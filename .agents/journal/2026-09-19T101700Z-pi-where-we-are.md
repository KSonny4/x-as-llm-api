# Journal: where we are (2026-09-19T1017Z, pi session)

## Live state
- Keeper matrix: 103 routes (33 keys + 70 zen free-model sweep), last full
  dispatch 89/89 reported. Zen free models degraded on all keys (correct:
  direct chat tier-denied, CLI passes); jev-1.13-free down everywhere
  (model gone upstream); 4× OpenRouter gpt-4o-mini ok.
- Active goal: keeper→pi-infinity-llm handover (M1+M3 committed, M2/M4
  quota-blocked, paused to 2026-09-20T00:30Z).

## Settled truths (measured, not claimed)
1. Free Zen keys: `/models` 200, direct-HTTP chat 403 FreeTierError on
   every network (laptop, keeper alloc, probe image, CF worker).
   Only the opencode CLI passes chat ("within OpenCode").
2. Python-urllib default UA 403s even `/models`; fleet UA
   (`keeper-probe/1.0`) fixes it. UA red herrings eliminated one by one
   (case, order, Accept-Encoding, family, TLS stack — all measured).
3. Mullvad sidecar routes correctly but shares one key+address with Pi
   gluetun (10.69.30.235/32 both ends) → blackhole. Parked; second key
   needs same-account proof + a device slot (5/5 used).
4. CF worker proxies fine (keyed 200) but moves the same denied request.
   Parked spare, deployed.
5. pi-opencode-direct (v0.1.6, pure HTTP, CLI-identity spoof) is the
   light vehicle: adopted, pinned, keyed — then Zen 429'd both Bao keys
   (~13.7h retry). Debug proves keyed requests with spoofed UA.
6. pi holds a DIFFERENT (paid?) zen key in its auth — pi chat worked
   through it while free keys 429. Untouched.

## Shipped
- probe fleet UA + test, python:3.12-slim image, 103-route render with
  per-model L2 refs, e2e report (docs/e2e-what-worked.md).
- pi-zen-bridge spike: FIT (text-only limit documented), e2e task green.
- pi-infinity-llm tools/: keeper-models-snippet.sh + translate.jq +
  shape test 12/12 + redacted sample; pi 0.85.1; pi-opencode-direct@0.1.6.
- Receipts: x-as-llm-api/docs/zen-egress-receipts/ (9 files).

## Blockers / next
- Zen 429 until ~00:30Z: M2 keyed chat proof + M4 fresh-apply e2e.
  Snippet staged in /tmp, agreement holds.
- L1-ok for free keys is impossible by vendor policy (no egress fix);
  recorded, auditor-ledgered.
- Owner-side only: second Mullvad key decision, paid-key purchase,
  Owner-cell filling, PR merges.

## Addendum 12:05Z — all keys verified good, Muse served to pi
- 10/10 zen keys models:200; 8/8 retired keyed-chat green (fresh
  per-key quota); 2 live quota-spent (429). Bao metadata refreshed
  (status=active, verified=2026-09-19, keys untouched, 67 chars).
- pi auth.json holds opencode-zen-free key (RETIRED_1, backup kept);
  `SERVED-MUSE-ALIVE` via stored credential, no env override.
