{
  "name": "pi-zen-bridge-spike",
  "version": "0.1.0",
  "private": true,
  "description": "M1 spike: pi provider shelling to the opencode CLI so free Zen keys work inside pi without HTTP.",
  "type": "module",
  "dependencies": {
    "@earendil-works/pi-ai": "latest",
    "@earendil-works/pi-coding-agent": "latest"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "test": "node --test test/bridge.test.mjs",
    "typecheck": "tsc --noEmit"
  }
}