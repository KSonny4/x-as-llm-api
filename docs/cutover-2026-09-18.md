# Hostname cutover + deprecations — 2026-09-18

Goal: cut `keeper.pkubelka.cz` to the Nomad job, remove the Coolify keeper
app, deprecate + archive llm-quota. No secret values below.

## Cutover (tunnel `coolify-admin` b145382e, remotely managed)

- DNS was already `CNAME keeper.pkubelka.cz → <tunnel>.cfargotunnel.com`.
- Nomad keeper pinned to static loopback **8102** (job v6, in-alloc smoke
  GREEN) following the cluster pattern (dump 8100/8101).
- Tunnel ingress via CF API: `keeper.pkubelka.cz: localhost:80 → 8102`.
  Remote-managed cloudflared picked it up in ~10s.
- Seeded one live route (job v7, `-var seeds_json` rendered from Bao at
  deploy, never in git): openrouter `openai/gpt-4o-mini`, openai wire.

Verification against the hostname (all green):

- `curl https://keeper.pkubelka.cz/healthz` → `200` (`ok`)
- authed `POST /v1/chat/completions` (`openai/gpt-4o-mini`, max_tokens 5)
  → `choices[0].message.content` = `Alive` (proves the full path:
  hostname → tunnel → Nomad alloc → OpenRouter → text back)
- `KEEPER_TOKEN=<new> bash scripts/smoke.sh https://keeper.pkubelka.cz`
  → 16/16 `ok`, zero FAIL (re-ran after Coolify removal: still green,
  packs show v2 + 1 seeded member)

## Coolify removal

- App `keeper` (`kv4gvawhgshjaxdggb2vgvuf`, built from pi-infinity-llm main,
  no FQDN, status running:unknown) deleted via Coolify API
  (`delete_configurations=true, delete_volumes=false, docker_cleanup=true`;
  volumes preserved). Access path: CF Access service token
  (`COOLIFY_ACCESS_SERVICE_TOKEN`) + `COOLIFY_API` token.
- Post-delete: app absent from list (8 remain), hostname still serves v2.
- Note: `llm-quota` / `llm-quota2` Coolify apps still exist (running) —
  outside this goal's scope; remove separately once the archived repo's
  retirement is confirmed.

## llm-quota deprecation

- `DEPRECATED.md` written (mapping table to keeper endpoints, parity
  evidence pointer, bookmark redirects), committed as `c71ed2c` on master
  (rebased onto `18445e3`), pushed.
- `gh repo archive KSonny4/llm-quota` → `isArchived: true` (verified).

## Follow-ups (not this goal)

- More live seed routes (currently exactly one); live Bao-export seed
  rotation story.
- Vault/Consul or workload identity so bearers leave job specs.
- Retire the `llm-quota*` Coolify apps + localhost:80 origin contents.
